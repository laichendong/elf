#include <stdio.h>

// 判断2000-2500年中每一年是否为闰年， 并输出结果
// 闰年  = 能被4整除但不能被100整除  or  能同时被100和400整除
main(){
    for(int i = 2000; i<= 2500; i++){
        if(((i % 4==0) && (i%100!=0)) || ((i%100==0) && (1%400)==0)){
            printf("%d年是闰年\n", i);
        }
        printf("%d年不是闰年\n", i);
    }
}